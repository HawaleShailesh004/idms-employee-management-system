{
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/uploads/(.*)", "destination": "/api/uploads/$1" }
  ],
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
